

enum AppStatus { idle, loading, success, error }

enum AuthStatus { idle, loading, success, error }

enum EmergencyStatus { idle, loading, success, error }

enum PatientGender { male, female }

enum PatientRelation { family, friend, other }
